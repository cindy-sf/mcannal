import React from 'react'

import Search from '..'

describe('Search', () => {
  afterEach(jest.clearAllMocks)

  describe('render', () => {
    describe('without query', () => {
      it('should render page correctly without query', () => {
        // GIVEN
        // THEN
      })
    })
  })
})
